const routes = Object.freeze({
  main: '/',
  buildCard: '/build',
  equipCard: '/equip',
  equipmentCard: '/equipment',
  generalCard: '/general',
  metalCard: '/metal',
  windowCard: '/windows',
})

export default routes
